# Вводные данные для новой сессии
**Дата создания:** 27.11.2025, 19:13  
**Последнее обновление:** 28 ноября 2025

---

## 🚨 ПРАВИЛА ИЗМЕНЕНИЯ КРИТИЧНЫХ ФАЙЛОВ

> [!CAUTION]
> **КРИТИЧНО:** При изменении `video-menu.sh` и `vlc-cec.sh` соблюдать строгие правила!

### Принцип минимальных изменений:
1. **ВСЕГДА делать бэкап** перед любым изменением
2. **Вносить МИНИМАЛЬНО возможные изменения**
3. **Подробно описывать** каждое изменение ПЕРЕД применением
4. **Ждать подтверждения** перед выполнением

### Процесс изменения:

```bash
# 1. Бэкап
TODAY=$(date +%y%m%d)
TIMESTAMP=$(date +%y%m%d_%H%M)
cp vlc-cec.sh BAK/$TODAY/vlc-cec_V#_${TIMESTAMP}.bak

# 2. Описать изменения
# - Что меняется
# - Почему меняется  
# - Какие строки затрагиваются
# - Ожидаемый результат

# 3. Получить подтверждение

# 4. Внести изменения

# 5. Проверить синтаксис
bash -n vlc-cec.sh

# 6. Протестировать
```

### Формат описания изменений:

```markdown
## Планируемые изменения в vlc-cec.sh

**Файл:** vlc-cec.sh
**Строки:** 50-55
**Цель:** Добавить автосохранение прогресса

**Что меняется:**
1. Добавляется source playback-tracker.sh (после строки 13)
2. Добавляется функция monitor_progress() (после строки 69)
3. Запуск мониторинга в фоне (после строки 68)
4. Обновление cleanup() для финального сохранения (строки 334-341)

**Ожидаемый результат:**
- VLC работает как раньше
- Каждые 60 сек сохраняется позиция в .playback
- При выходе - финальное сохранение

**Риски:** Минимальные, мониторинг в фоне

ЖДАТЬ ПОДТВЕРЖДЕНИЯ ПЕРЕД ВЫПОЛНЕНИЕМ!
```

---

## 🌐 Глобальные инструкции

### Язык коммуникации
- **Основной язык:** Русский
- Код и комментарии в коде: Русский
- Документация: Русский
- Commit messages и CHANGELOG: Русский

### Обязательные файлы для review в начале сессии
1. **HANDOFF-NEXT-SESSION.md** (этот файл) - ВСЕГДА просматривать ПОЛНОСТЬЮ
2. **CHANGELOG.md** - просматривать ТОЛЬКО при необходимости (например, перед обновлением)
3. **Summary_*.md** - технические отчёты (последний по дате) - при необходимости
4. Рабочие файлы (просматривать по мере надобности):
   - `vlc-cec.sh`
   - `video-menu.sh`
   - `playback-tracker.sh`

### Правила работы с документацией и контроль токенов

> [!IMPORTANT]
> **Контроль расхода токенов** критичен для успешного выполнения задач!

**Бюджет токенов:** 200,000 на сессию

**Правила оптимизации:**

1. **HANDOFF-NEXT-SESSION.md:**
   - ✅ ВСЕГДА просматривать полностью в начале сессии
   - Это главный источник контекста проекта

2. **CHANGELOG.md:**
   - ⚠️ Просматривать ТОЛЬКО когда нужно обновить или понять историю
   - НЕ читать автоматически в начале сессии
   - Большой файл (~280 строк) = много токенов

3. **Summary файлы:**
   - Просматривать только последний при необходимости
   - Не читать все подряд

4. **Контроль токенов при больших изменениях:**
   - Перед началом большой задачи - оценить расход токенов
   - Если планируется редактирование 3+ файлов или 200+ строк - проверить остаток токенов
   - **Критический порог:** < 20,000 токенов - прервать и начать новую сессию
   - **Предупреждение:** 20,000-50,000 токенов - завершить текущую задачу, не начинать новые
   - **Норма:** > 50,000 токенов - можно продолжать

5. **Оптимизация просмотра файлов:**
   - Использовать `StartLine/EndLine` для просмотра конкретных участков
   - Использовать `grep_search` вместо полного просмотра где возможно
   - Не смотреть файл повторно без необходимости

### Структура проекта

**Технологии:**
- Bash scripting
- VLC media player (cvlc)
- Dialog (TUI меню)
- CEC (Consumer Electronics Control) для управления через пульт ТВ
- Raspberry Pi 4 в console mode

### Система бэкапов

**Формат имён бэкапов:**
```
filename_V#_YYMMDD_HHMM.bak
```

**Компоненты:**
- `filename` - базовое имя без расширения
- `V#` - номер версии (V1, V2, V3...)
- `YYMMDD_HHMM` - дата и время (251127_2021)
- `.bak` - расширение бэкапа

**Примеры:**
```
vlc-cec_V5_251127_2021.bak
CHANGELOG_V1_251127_2021.bak
Summary_251127_1745_V1_251127_2021.bak
```

**Структура хранения:**
```
/vlc/
├── *.sh                    # Рабочие файлы (playback-tracker, video-menu, vlc-cec)
├── README.md               # Описание для GitHub (в корне)
├── backup-end-of-day.sh    # Скрипт бэкапов
├── RELIS/                  # Релизные версии
│   ├── playback-tracker_RELIS.sh
│   ├── video-menu_RELIS.sh
│   ├── vlc-cec_RELIS.sh
│   └── series-tracker_RELIS.sh (legacy)
├── DOCS/                   # Вся документация
│   ├── HANDOFF-NEXT-SESSION.md
│   ├── CHANGELOG.md
│   ├── Summary_*.md
│   └── future_features/
│       └── development_roadmap.md
├── BAK/                    # Папка бэкапов
│   ├── 251128/             # Бэкапы за 28.11.2025
│   │   ├── *_V#_*.bak      # .sh и .md файлы
│   │   └── Summary_*.md    # Копии Summary
│   └── ...
└── archive/
    └── old_backups_*/      # Архив старых форматов
```

**Расположение документации:**
- Вся текущая документация находится в `DOCS/`
- README.md остаётся в корне (для GitHub)
- Summary файлы создаются сразу в `DOCS/`

**Правила создания бэкапов:**

1. **В НАЧАЛЕ рабочего дня:**
   - Прочитать `DOCS/HANDOFF-NEXT-SESSION.md`
   - Создать `DOCS/Summary_YYMMDD_HHMM.md` (технический отчёт о плане работы)
   - Создать папку `BAK/YYMMDD/` если её нет

2. **ПЕРЕД изменениями рабочих файлов:**
   - Создать бэкап изменяемого файла
   - Нумерация V# продолжается от последнего в `BAK/`
   - Бэкап сразу поместить в `BAK/YYMMDD/`

3. **ПЕРЕД обновлением RELIS файлов (КРИТИЧНО!):**
   - ВСЕГДА создавать бэкап текущей RELIS версии ПЕРЕД перезаписью
   - Только после бэкапа копировать новую версию в `RELIS/`
   - Пример:
     ```bash
     cp RELIS/vlc-cec_RELIS.sh BAK/251128/vlc-cec_RELIS_V5_251128_1330.bak
     cp vlc-cec.sh RELIS/vlc-cec_RELIS.sh
     ```

4. **В КОНЦЕ рабочего дня (обязательно!):**
   - Создать бэкапы ВСЕХ RELIS файлов из `RELIS/` (даже без изменений)
   - Создать бэкапы .md файлов из `DOCS/`:
     - HANDOFF-NEXT-SESSION.md
     - CHANGELOG.md
     - development_roadmap.md
     - Summary_YYMMDD_*.md (текущие)
   - Все бэкапы поместить в `BAK/YYMMDD/`
   - Использовать: `./backup-end-of-day.sh`

5. **Архивирование:**
   - Старые форматы перемещать в `archive/old_backups_YYMMDD/`
   - Бэкапы старше 30 дней можно архивировать

**Команды для ежедневного workflow:**

```bash
# === НАЧАЛО ДНЯ ===
# 1. Создать папку для бэкапов
TODAY=$(date +%y%m%d)
mkdir -p BAK/$TODAY

# 2. Создать Summary в DOCS/
TIMESTAMP=$(date +%y%m%d_%H%M)
# vi DOCS/Summary_${TODAY}_${TIMESTAMP}.md

# 2. После создания Summary
TIMESTAMP=$(date +%y%m%d_%H%M)
cp Summary_${TODAY}_*.md BAK/$TODAY/Summary_${TODAY}_*_V1_${TIMESTAMP}.bak

# === ПЕРЕД ИЗМЕНЕНИЕМ ФАЙЛА ===
# 3. Определить последнюю версию (вручную или автоматически)
# Например, последний vlc-cec был V5, значит создаём V6
TIMESTAMP=$(date +%y%m%d_%H%M)
cp vlc-cec.sh BAK/$TODAY/vlc-cec_V6_${TIMESTAMP}.bak

# === ПЕРЕД ОБНОВЛЕНИЕМ RELIS ===
# КРИТИЧНО: Сначала бэкап, потом копирование!
TIMESTAMP=$(date +%y%m%d_%H%M)
cp RELIS/vlc-cec_RELIS.sh BAK/251128/vlc-cec_RELIS_V5_${TIMESTAMP}.bak
cp vlc-cec.sh RELIS/vlc-cec_RELIS.sh

# === КОНЕЦ ДНЯ ===
# 4. Автоматический бэкап через скрипт
./backup-end-of-day.sh

# Скрипт создаст бэкапы:
# - ВСЕХ RELIS файлов из RELIS/
# - ВСЕХ .md документов
# - Summary файлов за сегодня
```

**Автоматизация (скрипт backup-end-of-day.sh):**

Скрипт уже создан и обновлён для новой структуры:
```bash
#!/bin/bash
TODAY=$(date +%y%m%d)
TIMESTAMP=$(date +%y%m%d_%H%M)
mkdir -p BAK/$TODAY

echo "Создание бэкапов на конец дня..."

# RELIS файлы из папки RELIS/
cp RELIS/vlc-cec_RELIS.sh BAK/$TODAY/vlc-cec_RELIS_V#_${TIMESTAMP}.bak
cp RELIS/video-menu_RELIS.sh BAK/$TODAY/video-menu_RELIS_V#_${TIMESTAMP}.bak
cp RELIS/playback-tracker_RELIS.sh BAK/$TODAY/playback-tracker_RELIS_V#_${TIMESTAMP}.bak
cp RELIS/series-tracker_RELIS.sh BAK/$TODAY/series-tracker_RELIS_V#_${TIMESTAMP}.bak

# Документация .md из DOCS/
cp DOCS/HANDOFF-NEXT-SESSION.md BAK/$TODAY/HANDOFF-NEXT-SESSION_V#_${TIMESTAMP}.bak
cp DOCS/CHANGELOG.md BAK/$TODAY/CHANGELOG_V#_${TIMESTAMP}.bak
cp DOCS/future_features/development_roadmap.md BAK/$TODAY/development_roadmap_V#_${TIMESTAMP}.bak
cp DOCS/Summary_${TODAY}_*.md BAK/$TODAY/ 2>/dev/null

echo "✓ Бэкапы созданы в BAK/$TODAY/"
```

> [!IMPORTANT]
> V# необходимо определять вручную, посмотрев последний номер в `BAK/`!

### Система версионирования

- `filename.sh` - текущая рабочая версия
- `filename_RELIS.sh` - финальный релиз (протестированная версия)
- `filename_V#_YYMMDD_HHMM.bak` - бэкапы с историей версий

---

## ✅ Текущий статус проекта (27.11.2025)

### Что работает
- ✅ VLC проигрывание с CEC управлением через пульт ТВ
- ✅ Файловый менеджер с `dialog` для выбора видео
- ✅ Универсальное отслеживание прогресса для ВСЕХ видеофайлов (не только сериалов)
- ✅ Иконки статуса: `[ ]` не просмотрено, `[T]` частично, `[X]` просмотрено
- ✅ Сохранение/загрузка позиции воспроизведения в `.ser` файлы
- ✅ Возврат курсора в меню:
  - После навигации вверх (`..`) - курсор на папку
  - После воспроизведения - курсор на видеофайл
- ✅ Hotkey в меню - первая буква файла красная и активна для быстрого поиска
- ✅ Корректное отображение иконок статуса справа от названия файла

### Известные проблемы
- ⚠️ Переключение аудиодорожек (RED кнопка) - работает некорректно
- ⚠️ Переключение субтитров (GREEN кнопка) - работает некорректно
- ⚠️ Громкость (YELLOW/BLUE кнопки) - планируется переделать на другую функцию

---

## 📋 Незавершённые задачи с предыдущей сессии

### Средний приоритет
1. **Переименование `.ser` → `.playback`**
   - Обновить `playback-tracker.sh`:
     - Функция `get_ser_file` → `get_playback_file`
     - Изменить расширение с `.ser` на `.playback`
   - Миграция существующих `.ser` файлов
   - Обновить комментарии и документацию

2. **Рефакторинг комментариев**
   - Убрать упоминания "сериалов" из комментариев
   - Обновить на "видеофайлы" или "прогресс воспроизведения"
   - Файлы для обновления: `playback-tracker.sh`, `video-menu.sh`

3. **Cleanup кода**
   - Удалить отладочный вывод из `video-menu.sh`:
     ```bash
     # Строка 104-105
     echo "DEBUG: Вычисленная ширина окна: $max_width"
     ```
   - Пометить устаревшие функции как DEPRECATED:
     - `is_series_file` (больше не используется)
     - `get_episode_code` (больше не используется)

### Низкий приоритет
4. **Исправление CEC кнопок** (в vlc-cec.sh)
   - RED кнопка (переключение аудиодорожек) - требует отладки
   - GREEN кнопка (переключение субтитров) - требует отладки
   - YELLOW/BLUE кнопки - переделать с громкости на другую функцию (определить какую)

5. **Организация справочных файлов для разработки**
   - Создать структуру для хранения справок/подсказок
   - Возможно `DOCS/references/` или `DOCS/dev_notes/`
   - Включить VLC RC commands, CEC коды, и другие справки
   - Определить что идёт в Git, а что в .gitignore

6. **API для удалённого управления плеером (долгосрочная)**
   - Разработать HTTP/REST API для управления VLC
   - Endpoints: play, pause, seek, get_status, get_progress
   - Цель: управление с мобильных приложений (iOS/Android)
   - Безопасность: авторизация, HTTPS
   - Интеграция с playback-tracker.sh
   - Возможно использовать VLC HTTP интерфейс как основу

---

## 📂 Структура файлов проекта

### Активные (рабочие)
```
vlc-cec.sh              - VLC плеер с CEC + поддержка старта с позиции
video-menu.sh           - Файловый менеджер с универсальным отслеживанием
playback-tracker.sh     - Библиотека отслеживания для всех видеофайлов
series-tracker.sh       - Библиотека (legacy, для обратной совместимости)
```

### Релизные версии (_RELIS)
```
vlc-cec_RELIS.sh        - Стабильная версия VLC (Version 4)
video-menu_RELIS.sh     - Стабильная версия меню с сериалами
series-tracker_RELIS.sh - Стабильная версия библиотеки сериалов
```

### Документация
```
CHANGELOG.md                  - История изменений проекта
HANDOFF-NEXT-SESSION.md      - Этот файл (вводные данные)
Summary_251127_1745.md       - Технический отчёт от 27.11.2025
HANDOFF_bak_251127_1912.md   - Backup HANDOFF от 27.11.2025 19:12
```

### Data файлы
```
.ser или .playback  - Файлы прогресса воспроизведения
```

**Формат .ser/.playback:**
```
filename:seconds:total:percent:timestamp
```

---

## 🔧 Технические детали

### VLC параметры запуска
```bash
# Без сохранённой позиции:
./vlc-cec.sh "/path/to/file.avi"

# С сохранённой позицией:
./vlc-cec.sh "1234" "/path/to/file.avi"
```

### Dialog параметры
```bash
dialog --colors --output-fd 1 \
    --title "$title" \
    --default-item "$default_item" \
    --menu "Выберите файл или папку:" 20 120 15 \
    "${items[@]}" \
    2>/dev/tty
```

**Структура items:**
- Tag (первое поле) = чистое имя файла → hotkey на первой букве
- Description (второе поле) = иконка статуса + размер

### Цветовые коды Dialog
- `\Z4` - синий (не используется в текущей версии)
- `\Z1` - красный (автоматически для первой буквы hotkey)
- `\Zn` - сброс цвета

**Важно:** Dialog автоматически подсвечивает первый символ tag красным как hotkey. Попытки добавить цвета вручную через `\Z` не работали корректно.

### Пороги процентов просмотра
```bash
WATCHED_THRESHOLD=99     # >= 99% = [X]
PARTIAL_THRESHOLD=1      # >= 1%  = [T]
                         # 0%      = [ ]
```

### CEC коды кнопок
```
44:00 - OK (Play/Pause)
44:01 - UP (+30 sec)
44:02 - DOWN (-30 sec)
44:03 - LEFT (-10 sec)
44:04 - RIGHT (+10 sec)
44:0d - BACK (Exit)
44:35 - INFO (Time)
44:72 - RED (Audio track)
44:73 - GREEN (Subtitles)
44:74 - YELLOW (Volume +)
44:71 - BLUE (Volume -)
44:30 - CH UP (+60 sec)
44:31 - CH DOWN (-60 sec)
44:20-29 - Цифры 0-9 (Jump to %)
```

---

## 🚀 Рекомендации для новой сессии

### Перед началом работы
1. Просмотреть CHANGELOG.md для понимания последних изменений
2. Проверить текущие рабочие файлы: `vlc-cec.sh`, `video-menu.sh`, `playback-tracker.sh`
3. Обновить CHANGELOG.md при внесении изменений
4. Создавать backup версии перед критическими изменениями

### При создании новых релизов
1. Протестировать все функции
2. Создать `_RELIS.sh` версию только после успешного тестирования
3. Обновить CHANGELOG.md с описанием изменений
4. Обновить этот HANDOFF файл с текущим статусом

### При обнаружении проблем
1. Добавить в раздел "Известные проблемы" этого файла
2. Создать issue в разделе "Незавершённые задачи"
3. Обновить CHANGELOG.md если проблема критическая

---

## 📝 Checklist перед завершением сессии

- [ ] Обновлён CHANGELOG.md с новыми изменениями
- [ ] Создан backup HANDOFF файла (если были изменения)
- [ ] Обновлён этот HANDOFF с текущим статусом
- [ ] Созданы backup версии изменённых файлов
- [ ] Протестирована базовая функциональность
- [ ] Документированы известные проблемы
- [ ] Обновлён список незавершённых задач

---

**Контакт:** babayka  
**Платформа:** Raspberry Pi 4, Raspbian console mode  
**VLC версия:** cvlc (командная строка)  
**CEC устройство:** /dev/cec1
