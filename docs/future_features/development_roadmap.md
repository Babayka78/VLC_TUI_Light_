# План развития проекта VLC Media Player

**Дата:** 27.11.2025, 20:26  
**Статус:** Планирование следующих шагов

---

## 1. Интеграция с GitHub

### Преимущества
- ✅ Полная история изменений (git log)
- ✅ Возможность отката к любой версии
- ✅ Резервное копирование в облаке
- ✅ Возможность работы с разных устройств
- ✅ Профессиональный подход к разработке
- ✅ README.md для документации

### Шаги по настройке

#### Этап 1: Инициализация локального репозитория

```bash
cd /Users/babayka/PycharmProjects/vlc

# Инициализировать Git
git init

# Настроить имя и email
git config user.name "Ваше Имя"
git config user.email "your.email@example.com"
```

#### Этап 2: Создание .gitignore

**Что исключить:**
```
# Бэкапы (хранятся отдельно в BAK/)
*.bak

# Архивы
archive/

# Данные прогресса
.ser
.playback

# Временные файлы
*.tmp
*.swp
*~

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
```

#### Этап 3: Структура коммитов

**Recommended strategy:**
```bash
# 1. Первый коммит - текущее стабильное состояние
git add *.sh *_RELIS.sh *.md
git commit -m "Initial commit: VLC Media Player with CEC control

- vlc-cec.sh: VLC player с CEC управлением
- video-menu.sh: Файловый менеджер с отслеживанием прогресса
- playback-tracker.sh: Библиотека универсального отслеживания
- HANDOFF, CHANGELOG: Документация
- backup-end-of-day.sh: Скрипт автоматизации бэкапов"

# 2. Создать .gitignore
git add .gitignore
git commit -m "Add .gitignore: exclude backups and temp files"

# 3. Создать README.md (см. ниже)
git add README.md
git commit -m "Add README.md: project documentation"
```

#### Этап 4: Создание GitHub репозитория

1. **Зайти на GitHub.com** → New Repository
2. **Название:** `vlc-media-player-cec`
3. **Описание:** "VLC Media Player with CEC remote control and playback tracking for Raspberry Pi"
4. **Приватный/Публичный:** Выбрать (рекомендую Private для личного проекта)
5. **НЕ создавать** README, .gitignore (уже есть локально)

#### Этап 5: Подключение к GitHub

```bash
# Добавить remote (заменить USERNAME на ваш GitHub username)
git remote add origin https://github.com/USERNAME/vlc-media-player-cec.git

# Первый push
git branch -M main
git push -u origin main
```

#### Этап 6: Workflow с Git

**Ежедневная работа:**
```bash
# Начало дня - pull последних изменений (если работаете с разных устройств)
git pull

# Перед изменениями - создать бэкап (как обычно)
cp vlc-cec.sh BAK/$(date +%y%m%d)/vlc-cec_V#_$(date +%y%m%d_%H%M).bak

# После изменений - commit
git add vlc-cec.sh
git commit -m "Fix: VLC start position parameter parsing"

# Конец дня - push в GitHub
./backup-end-of-day.sh  # Бэкапы
git add .
git commit -m "End of day: $(date +%Y-%m-%d)"
git push
```

---

## 2. Незавершённые задачи (из HANDOFF)

### Высокий приоритет

#### 2.1 Переименование `.ser` → `.playback`
**Зачем:** Универсальное название (не только для сериалов)

**Шаги:**
1. Обновить `playback-tracker.sh`:
   - `get_ser_file()` → `get_playback_file()`
   - `SER_PATTERN=".ser"` → `PLAYBACK_PATTERN=".playback"`
   - Обновить комментарии
2. Миграция существующих файлов:
   ```bash
   find ~/mac_disk -name "*.ser" -exec rename 's/\.ser$/\.playback/' {} \;
   ```
3. Обновить `.gitignore` (`.ser` → `.playback`)
4. Commit: `"Refactor: rename .ser to .playback files"`

**Риски:** Потеря прогресса если миграция пойдет не так → сначала backup!

#### 2.2 Рефакторинг комментариев
**Зачем:** Убрать упоминания "сериалов", обновить на "видеофайлы"

**Файлы:**
- `playback-tracker.sh`
- `video-menu.sh`
- Возможно `vlc-cec.sh`

**Примеры:**
```bash
# Было:
# Сохранение прогресса для сериалов

# Стало:
# Сохранение прогресса для видеофайлов
```

### Средний приоритет

#### 2.3 Cleanup кода

**Что сделать:**
1. Удалить отладочный вывод в `video-menu.sh` (строка ~104):
   ```bash
   echo "DEBUG: Вычисленная ширина окна: $max_width"
   ```

2. Пометить устаревшие функции:
   ```bash
   # DEPRECATED: Больше не используется, оставлено для совместимости
   is_series_file() {
       # ...
   }
   ```

3. Удалить неиспользуемые функции (если есть)

### Низкий приоритет

#### 2.4 Исправление CEC кнопок (vlc-cec.sh)

**Проблемы:**
- RED кнопка (аудиодорожки) - работает некорректно
- GREEN кнопка (субтитры) - работает некорректно
- YELLOW/BLUE - переделать на другую функцию

**Предложения для YELLOW/BLUE:**
- Zoom in/out
- Aspect ratio change
- Sleep timer (через 30 мин выключить)
- Bookmark position

---

## 3. Новые возможности

### 3.1 README.md для GitHub

**Создать профессиональное описание проекта:**

```markdown
# VLC Media Player with CEC Control

VLC медиаплеер для Raspberry Pi с управлением через CEC пульт ТВ и отслеживанием прогресса просмотра.

## Возможности
- 🎮 Управление через пульт ТВ (CEC)
- 📊 Отслеживание прогресса всех видеофайлов
- 🔄 Автоматическое возобновление с момента остановки
- 📁 Удобный файловый менеджер (dialog)
- 🎯 Hotkeys для быстрого поиска
- 💾 Система бэкапов

## Требования
- Raspberry Pi 4
- Raspbian (console mode)
- VLC (cvlc)
- dialog
- cec-client

## Установка
[инструкции]

## Использование
[примеры]
```

### 3.2 Автоматизация тестирования

**Создать `test-all.sh`:**
```bash
#!/bin/bash
# Проверка синтаксиса всех скриптов

echo "Проверка синтаксиса..."
for file in *.sh; do
    if bash -n "$file" 2>&1; then
        echo "✓ $file"
    else
        echo "✗ $file - ОШИБКА!"
    fi
done
```

### 3.3 Логирование

**Добавить систему логов:**
```bash
# В vlc-cec.sh
LOG_DIR="$HOME/vlc/logs"
mkdir -p "$LOG_DIR"
LOG_FILE="$LOG_DIR/vlc_$(date +%Y%m%d).log"

log() {
    echo "[$(date +%H:%M:%S)] $1" | tee -a "$LOG_FILE"
}

log "VLC started: $VIDEO_FILE"
```

### 3.4 Статистика просмотра

**Создать `stats.sh`:**
```bash
#!/bin/bash
# Анализ статистики просмотра

echo "=== Статистика просмотра ==="
echo "Всего видеофайлов: $(find ~/mac_disk -name "*.playback" | wc -l)"
echo "Просмотрено полностью: [логика подсчета 99%+]"
echo "Частично просмотрено: [логика подсчета 1-98%]"
echo "Не просмотрено: [логика подсчета 0%]"
```

### 3.5 Конфигурационный файл

**Создать `config.conf`:**
```bash
# Настройки VLC Media Player

# Пути
VIDEO_DIR="$HOME/mac_disk"
CEC_DEVICE="/dev/cec1"

# Пороги процентов
WATCHED_THRESHOLD=99
PARTIAL_THRESHOLD=1

# Автоматические действия
AUTO_RESUME=true
AUTO_BACKUP=true

# Логирование
ENABLE_LOGGING=true
LOG_LEVEL="INFO"
```

---

## 4. Приоритизация

### Сделать в первую очередь

1. **✅ Git/GitHub интеграция** (сегодня!)
   - Инициализация репозитория
   - Создание .gitignore
   - Первый commit
   - Подключение к GitHub
   - Создание README.md

2. **🔄 Переименование .ser → .playback**
   - Refactoring кода
   - Миграция файлов
   - Тестирование

3. **📝 Cleanup кода**
   - Удаление debug
   - Рефакторинг комментариев
   - DEPRECATED метки

### Сделать потом

4. **📊 Новые функции**
   - test-all.sh
   - stats.sh
   - Логирование

5. **🔧 Исправление CEC**
   - Отладка RED/GREEN
   - Переработка YELLOW/BLUE

---

## 5. Рекомендуемый план на завтра

**Утро:**
1. Прочитать HANDOFF
2. Создать Summary_251128_HHMM.md
3. Инициализировать Git
4. Создать .gitignore
5. Первый commit

**День:**
6. Создать GitHub репозиторий
7. Push в GitHub
8. Создать README.md
9. Начать переименование .ser → .playback

**Вечер:**
10. Запустить backup-end-of-day.sh
11. Git commit + push
12. Обновить HANDOFF для следующей сессии

### 3.6 Плейлист

**Создать систему плейлистов:**
```bash
# Возможность создавать и сохранять списки воспроизведения
# - Создание плейлиста из выбранных файлов
# - Сохранение в .m3u или кастомный формат
# - Последовательное воспроизведение
# - Переход к следующему файлу автоматически
```

**Функционал:**
- Создание нового плейлиста из меню
- Добавление файлов в плейлист
- Управление порядком воспроизведения
- Сохранение/загрузка плейлистов
- Режим shuffle

### 3.7 Управление командами из меню

**Проблема:** Сейчас команды плееру отправляются через отдельный терминал с nc

**Решение:**
- Интегрировать управление командами непосредственно в меню video-menu.sh
- Использовать named pipe (FIFO) или socket для коммуникации с VLC
- Добавить пункты меню для управления:
  - Пауза/продолжить
  - Перемотка
  - Остановка воспроизведения
  - Переключение дорожек/субтитров
  - Регулировка громкости

**Техническая реализация:**
```bash
# Создать FIFO для коммуникации
mkfifo /tmp/vlc-control

# В vlc-cec.sh принимать команды из FIFO
# В video-menu.sh отправлять команды в FIFO
```

> [!WARNING]
> **FIFO корректно не работает** - обнаружены проблемы с взаимодействием через named pipes. Необходимо исследовать альтернативные подходы (HTTP интерфейс VLC, Unix sockets, или screen/tmux session sharing).

### 3.8 Решение проблемы с командой quit

**Проблема:** Команда quit перехватывается nc и не доходит до плеера

**Возможные решения:**

1. **Использовать escape-последовательность:**
   ```bash
   # Отправлять специальную команду типа "player_quit"
   # которую nc не распознает как свою команду
   ```

2. **Использовать альтернативный метод коммуникации:**
   ```bash
   # Вместо nc использовать named pipe (FIFO)
   # или Unix domain socket
   ```

3. **Обёртка для nc:**
   ```bash
   # Создать wrapper который фильтрует команды
   # quit → stop (для VLC)
   # real_quit → выход из nc
   ```

4. **Использовать HTTP интерфейс VLC:**
   ```bash
   # cvlc --http-host localhost --http-port 8080
   # Управление через curl
   curl http://localhost:8080/requests/status.xml?command=pl_stop
   ```

**Рекомендация:** Протестировать HTTP интерфейс VLC или Unix domain sockets (FIFO показал себя ненадёжным)

---

## Вопросы для обсуждения

1. **GitHub:**
   - Приватный или публичный репозиторий?
   - Какое название репозитория предпочитаете?

2. **Переименование .ser:**
   - Сделать прямо сейчас или после Git setup?
   - Нужен ли скрипт миграции?

3. **CEC кнопки:**
   - Что делать с YELLOW/BLUE? Какую функцию хотите?
   - Стоит ли трогать RED/GREEN или оставить как есть?

4. **Новые функции:**
   - Какие из предложенных интересны?
   - Есть ли свои идеи?
