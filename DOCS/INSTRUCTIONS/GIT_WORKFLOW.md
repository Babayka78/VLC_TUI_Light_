# GIT_WORKFLOW - Ежедневная работа с Git и GitHub

**Версия:** 1.0  
**Дата:** 01.12.2025

---

## 🎯 Стратегия: Промежуточная работа + Релизы

**Основной принцип:**
- GitHub = резервная копия + история разработки
- Регулярные коммиты и push (конец дня, после функции)
- Релизы = теги на стабильных коммитах

---

## 📅 Ежедневный workflow

### Конец рабочего дня

**1. Проверить статус:**
```bash
git status
```

**2. Добавить изменения:**
```bash
# Все файлы:
git add .

# Или выборочно:
git add vlc-cec.sh video-menu.sh DOCS/
```

**3. Коммит с осмысленным сообщением:**
```bash
# Для незавершённой работы:
git commit -m "WIP: Работа над SQLite интеграцией"

# Для завершённой фичи:
git commit -m "feat: Добавлено автосохранение прогресса каждые 60 секунд"

# Для багфикса:
git commit -m "fix: Исправлен extract_series_key в db-manager.sh"
```

**4. Push на GitHub:**
```bash
git push origin main
```

---

## 📝 Форматы commit messages

### Рекомендуемые префиксы:

- `feat:` - новая функциональность
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `style:` - форматирование, отступы
- `WIP:` - незавершённая работа (Work In Progress)

### Примеры:

```bash
git commit -m "feat: Добавлена настройка skip intro для сериалов"
git commit -m "fix: Исправлена ошибка возврата курсора в меню"
git commit -m "docs: Обновлён CHANGELOG для версии 0.6.0"
git commit -m "refactor: Вынесена функция save_progress в отдельный модуль"
git commit -m "WIP: Экспериментирую с новым UI для настроек"
```

---

## 🏷️ Релизы (теги)

### Когда создавать релиз:

- ✅ Завершена и протестирована новая функция
- ✅ Все изменения задокументированы в CHANGELOG
- ✅ Версия в файлах обновлена
- ✅ Код стабилен

### Процесс создания релиза:

См. детальные инструкции в [RELEASE.md](./RELEASE.md)

**Краткая версия:**
```bash
# 1. Убедиться что всё закоммичено
git status

# 2. Создать тег
git tag -a v0.6.0 -m "Версия 0.6.0 - SQLite интеграция"

# 3. Push тега
git push origin v0.6.0
```

---

## 🔄 Типичные ситуации

### Несколько изменений за день

```bash
# Утро: работа над функцией A
git add feature-a.sh
git commit -m "WIP: Функция A - базовая структура"

# День: завершение функции A
git add feature-a.sh
git commit -m "feat: Реализована функция A"
git push origin main

# Вечер: начало функции B
git add feature-b.sh
git commit -m "WIP: Функция B - начало работы"
git push origin main
```

### Забыли что-то добавить в коммит

```bash
# Добавить файл
git add forgotten-file.sh

# Исправить последний коммит
git commit --amend --no-edit

# Force push (ТОЛЬКО если не пушили ещё!)
git push origin main --force
```

### Откат к предыдущему коммиту

```bash
# Посмотреть историю
git log --oneline -10

# Откатиться (мягкий откат, изменения остаются)
git reset --soft HEAD~1

# Откатиться (жёсткий откат, изменения удаляются!)
git reset --hard HEAD~1
```

---

## 🛡️ Резервное копирование

**Двойная защита:**

1. **Локальные бэкапы:** `BAK/YYMMDD/`
2. **GitHub:** Полная история коммитов
3. **Артефакты:** `DOCS/Brain/YYMMDDHHMM/`

**Восстановление:**
```bash
# Склонировать с GitHub на новую машину
git clone https://github.com/username/vlc.git

# Или обновить существующий репозиторий
git pull origin main
```

---

## 📊 Просмотр истории

```bash
# Последние 10 коммитов
git log --oneline -10

# История конкретного файла
git log --oneline -- vlc-cec.sh

# Показать изменения в коммите
git show <commit-hash>

# Сравнить с предыдущей версией
git diff HEAD~1 vlc-cec.sh
```

---

## ⚠️ Важные правила

1. **Коммитить часто** - лучше 10 маленьких коммитов, чем 1 большой
2. **Осмысленные сообщения** - "fix stuff" ❌, "fix: Исправлен extract_series_key" ✅
3. **Push ежедневно** - GitHub = резервная копия
4. **Не коммитить:**
   - `.playback` файлы
   - Системные файлы (`.DS_Store`)
   - Временные файлы
   - Credentials

5. **Использовать .gitignore:**
```bash
# .gitignore
*.playback
.DS_Store
*.tmp
vlc_media.db
```

---

## 🚀 Автоматизация

### Скрипт git-push-daily.sh

Для быстрого push в конце дня:
```bash
./git-push-daily.sh "WIP: Работа над SQLite"
```

См. файл `git-push-daily.sh` в корне проекта.

---

## 🔗 Связанные документы

- [RELEASE.md](./RELEASE.md) - Процесс создания релизов
- [SESSION-END.md](./SESSION-END.md) - Checklist окончания дня
- [BACKUP_GUIDE.md](./BACKUP_GUIDE.md) - Система бэкапов

---

**Помните:** GitHub - это ваша резервная копия и портфолио. Коммитьте часто! 🚀
