# SESSION-END - Инструкции для завершения рабочего дня

**Версия:** 2.0 (с поддержкой обеих IDE)
**Дата:** 15.12.2025

---

> [!NOTE]
> **📚 Навигация:** [INDEX.md](./INDEX.md) | [HANDOFF.md](./HANDOFF.md) | [SESSION-START.md](./SESSION-START.md)

---

## 🔍 ШАГ 0: Определение окружения

> [!IMPORTANT]
> **ПЕРЕД выполнением остального - ответь в чате:**
>
> **Какое окружение ты используешь?**
> - Напиши: `A)` если работаешь в **Antigravity (Google Gemini)**
> - Напиши: `B)` если работаешь в **Claude Code (VSCode Extension)**
>
> Я выполню инструкции для выбранного окружения

---

## 📋 Checklist завершения дня (Универсальная часть)

### 1. Копирование артефактов текущей сессии

> [!NOTE]
> Артефакты копируются в `DOCS/Brain/` (документация), НЕ в `BAK/` (бэкапы кода)

**Команда для агента:**

```bash
# Определить текущую session ID
session_id=$(ls -t ~/.gemini/antigravity/brain | head -1)
timestamp=$(date +%y%m%d%H%M)

# Создать папку для артефактов в DOCS/Brain/
mkdir -p DOCS/Brain/$timestamp

# Копировать ВСЕ артефакты (полная история!)
cp ~/.gemini/antigravity/brain/$session_id/*.md DOCS/Brain/$timestamp/ 2>/dev/null
cp ~/.gemini/antigravity/brain/$session_id/*.md.resolved* DOCS/Brain/$timestamp/ 2>/dev/null
cp ~/.gemini/antigravity/brain/$session_id/*.md.metadata.json DOCS/Brain/$timestamp/ 2>/dev/null

echo "✓ Артефакты → DOCS/Brain/$timestamp/"
```

**Что копируется:**
- ✅ Все `.md` файлы (task.md, implementation_plan.md, walkthrough.md и т.д.)
- ✅ Все `.md.resolved.*` (история изменений)
- ✅ Все `.md.metadata.json` (комментарии пользователя!)
- ❌ Изображения НЕ копируются

---

### 2. Обновление HANDOFF.md

Если были значимые изменения:

```markdown
## ✅ Текущий статус проекта (ДД.ММ.ГГГГ)

### Что работает
- [Обновить список]

### 🔄 В процессе
- [Обновить текущие задачи]

### Известные проблемы
- [Добавить новые или убрать решённые]

## 📋 Незавершённые задачи
- [Обновить список]
```

---

### 3. Обновление CHANGELOG.md

Если были значимые изменения в коде:

```markdown
## [Unreleased]

### Added
- Новая функция X

### Changed
- Изменена логика Y

### Fixed
- Исправлена ошибка Z
```

---

### 4. Запуск backup-end-of-day.sh

> [!IMPORTANT]
> Скрипт работает с подтверждением - показывает список файлов перед копированием

```bash
./backup-end-of-day.sh
```

**Что бэкапится:**
- Документация (HANDOFF, CHANGELOG) - ТОЛЬКО если изменилась
- development_roadmap.md - ТОЛЬКО если изменился
- RELIS файлы НЕ бэкапятся (только при релизе!)

---

### 5. Финальная проверка

- [ ] Артефакты скопированы в `DOCS/Brain/YYMMDDHHMM/`
- [ ] HANDOFF.md обновлён с текущим статусом
- [ ] CHANGELOG.md обновлён (если были изменения)
- [ ] `backup-end-of-day.sh` выполнен
- [ ] Все критичные файлы сохранены

---

## 🔄 Процесс закрытия сессии

```bash
# 1. Копирование артефактов в DOCS/Brain/ (документация)
echo "Копирование артефактов..."
session_id=$(ls -t ~/.gemini/antigravity/brain | head -1)
timestamp=$(date +%y%m%d%H%M)
mkdir -p DOCS/Brain/$timestamp
cp ~/.gemini/antigravity/brain/$session_id/*.md* DOCS/Brain/$timestamp/ 2>/dev/null
echo "✓ Артефакты → DOCS/Brain/$timestamp/"

# 2. Бэкап изменённой документации в BAK/ (бэкапы кода)
echo "Запуск backup-end-of-day.sh..."
./backup-end-of-day.sh

# 3. Готово!
echo "✅ Сессия завершена!"
echo ""
echo "📂 Артефакты сессии: DOCS/Brain/$timestamp/"
echo "💾 Бэкапы кода/инструкций: BAK/$(date +%y%m%d)/"
```

---

## 📝 Примечания

**Структура хранения:**
- **DOCS/Brain/YYMMDDHHMM/** - артефакты сессий (документация)
- **BAK/YYMMDD/** - бэкапы кода и инструкций
- **BAK/RELIS/YYMMDD/** - бэкапы релизов

**Правила:**
- Артефакты копируются ВСЕГДА при закрытии сессии → `DOCS/Brain/`
- Дата/время папки = время ЗАКРЫТИЯ сессии
- Полная история сохраняется для демонстрации мыслительного процесса
- Документация бэкапится только если изменилась → `BAK/YYMMDD/INSTRUCTIONS/`
- RELIS файлы бэкапятся ТОЛЬКО при релизе → `BAK/RELIS/YYMMDD/`

---

См. также:
- [HANDOFF.md](./HANDOFF.md) - Главный файл проекта
- [SESSION-START.md](./SESSION-START.md) - Инструкции начала дня
- [BACKUP_GUIDE.md](./BACKUP_GUIDE.md) - Детальное руководство по бэкапам
