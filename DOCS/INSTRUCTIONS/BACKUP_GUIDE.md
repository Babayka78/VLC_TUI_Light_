# BACKUP_GUIDE - Руководство по созданию бэкапов

**Версия:** 1.0  
**Дата:** 01.12.2025

> [!NOTE]
> Это практическое руководство. Философию и правила см. в [BACKUP_POLICY.md](./BACKUP_POLICY.md)

---

## 📂 Структура бэкапов

```
BAK/
├── YYMMDD/                  # Промежуточные бэкапы
│   └── filename_YYMMDD_HHMM.bak
└── RELIS/                   # Бэкапы релизов
    └── YYMMDD/
        └── filename_RELIS_YYMMDD_HHMM.bak
```

---

## 🔧 Типы бэкапов

### 1. Промежуточные бэкапы (в течение дня)

**Когда:** Перед изменением критичных файлов

**Команда для агента:**
```bash
TODAY=$(date +%y%m%d)
TIMESTAMP=$(date +%y%m%d_%H%M)
mkdir -p BAK/$TODAY

# Спросить разрешение у пользователя!
echo "Создать бэкап vlc-cec.sh → BAK/$TODAY/vlc-cec_${TIMESTAMP}.bak?"
# После подтверждения:
cp vlc-cec.sh BAK/$TODAY/vlc-cec_${TIMESTAMP}.bak
```

**Важно:**
- ✅ Агент **ОБЯЗАН** спрашивать разрешение перед созданием
- ✅ Без V# в названии (только дата_время)
- ✅ Только изменяемые файлы

---

### 2. Бэкапы при закрытии сессии

**Когда:** В конце каждого рабочего дня

**Команда:**
```bash
./backup-end-of-day.sh
```

**Скрипт бэкапит:**
- Документация (HANDOFF, CHANGELOG) - только если изменились
- development_roadmap.md - только если изменился
- **НЕ** бэкапит RELIS файлы (они бэкапятся только при релизе!)

---

### 3. Бэкапы при релизе

**Когда:** При обновлении RELIS файлов

**Команда:**
```bash
TODAY=$(date +%y%m%d)
TIMESTAMP=$(date +%y%m%d_%H%M)
mkdir -p BAK/RELIS/$TODAY

# ОБЯЗАТЕЛЬНО: Сначала бэкап, потом обновление!
cp RELIS/vlc-cec_RELIS.sh BAK/RELIS/$TODAY/vlc-cec_RELIS_${TIMESTAMP}.bak
cp vlc-cec.sh RELIS/vlc-cec_RELIS.sh
```

**Важно:**
- ✅ Бэкап RELIS **ВСЕГДА обязателен** перед обновлением
- ✅ Бэкапы релизов хранятся отдельно в `BAK/RELIS/`

---

## 💾 Артефакты сессий

> [!NOTE]
> Артефакты хранятся в `DOCS/Brain/` (это документация), НЕ в `BAK/` (бэкапы кода)

**Когда:** При закрытии каждой сессии

**Автоматическая команда:**
```bash
session_id=$(ls -t ~/.gemini/antigravity/brain | head -1)
timestamp=$(date +%y%m%d%H%M)

# Создаём папку в DOCS/Brain/ (документация)
mkdir -p DOCS/Brain/$timestamp

# Копировать ВСЕ (включая историю)
cp ~/.gemini/antigravity/brain/$session_id/*.md DOCS/Brain/$timestamp/ 2>/dev/null
cp ~/.gemini/antigravity/brain/$session_id/*.md.resolved* DOCS/Brain/$timestamp/ 2>/dev/null
cp ~/.gemini/antigravity/brain/$session_id/*.md.metadata.json DOCS/Brain/$timestamp/ 2>/dev/null

echo "✓ Артефакты → DOCS/Brain/$timestamp/"
```

**Копируется:**
- ✅ Все `.md` файлы
- ✅ Вся история (`.resolved.*`)
- ✅ Комментарии (`.metadata.json`)
- ❌ Изображения НЕ копируются

**Структура:**
```
DOCS/Brain/2512011752/
├── task.md
├── task.md.resolved.0
├── implementation_plan.md
├── implementation_plan.md.resolved.0
├── implementation_plan.md.metadata.json  ← Ваши комментарии!
└── session_summary.md
```

---

## 🔢 Версионность

### V# в бэкапах vs версия релиза

> [!CAUTION]
> **Это разные вещи!**

**Промежуточные бэкапы:**
```
BAK/251201/vlc-cec_251201_1530.bak    # Без V#
```

**Бэкапы релизов:**
```
BAK/RELIS/251201/vlc-cec_RELIS_251201_1800.bak
```

**Версия релиза** хранится **внутри файла:**
```bash
# vlc-cec.sh
# Version: 0.5.0    ← Версия релиза
```

---

## 📋 Быстрые команды

### Промежуточный бэкап
```bash
TODAY=$(date +%y%m%d)
TIMESTAMP=$(date +%y%m%d_%H%M)
mkdir -p BAK/$TODAY
cp filename.sh BAK/$TODAY/filename_${TIMESTAMP}.bak
```

### Бэкап перед RELIS
```bash
TODAY=$(date +%y%m%d)
TIMESTAMP=$(date +%y%m%d_%H%M)
mkdir -p BAK/RELIS/$TODAY
cp RELIS/filename_RELIS.sh BAK/RELIS/$TODAY/filename_RELIS_${TIMESTAMP}.bak
```

### Копирование артефактов
```bash
timestamp=$(date +%y%m%d%H%M)
session_id=$(ls -t ~/.gemini/antigravity/brain | head -1)
mkdir -p DOCS/Brain/$timestamp
cp ~/.gemini/antigravity/brain/$session_id/*.md* DOCS/Brain/$timestamp/ 2>/dev/null
```

---

## ⚠️ Важные правила

1. **Промежуточные бэкапы** - ВСЕГДА спрашивать разрешение
2. **RELIS бэкапы** - ВСЕГДА создавать перед обновлением
3. **Артефакты** - копировать при КАЖДОМ закрытии сессии в `DOCS/Brain/`
4. **Без V#** в промежуточных бэкапах (только дата_время)
5. **Версия релиза** хранится внутри файла, не в названии

**Структура:**
- `DOCS/Brain/YYMMDDHHMM/` - артефакты сессий (документация)
- `BAK/YYMMDD/` - промежуточные бэкапы кода
- `BAK/YYMMDD/INSTRUCTIONS/` - бэкапы инструкций (если менялись)
- `BAK/RELIS/YYMMDD/` - бэкапы релизов

---

См. также:
- [BACKUP_POLICY.md](./BACKUP_POLICY.md) - Философия и правила бэкапов
- [SESSION-END.md](./SESSION-END.md) - Инструкции окончания дня
- [RELEASE.md](./RELEASE.md) - Процесс релиза
