# RELEASE - Процесс выпуска релиза

**Версия:** 1.0  
**Дата:** 01.12.2025  
**Статус:** ✅ Утверждено

---

## 📋 Система версионирования

### Формат: Semantic Versioning (semver)

**Структура:** `MAJOR.MINOR.PATCH`

**Пример:** `0.5.2`

---

### 🔢 Правила изменения версии

#### MAJOR (первое число)
**Когда увеличивать:**
- Ломающие изменения (breaking changes)
- Несовместимость с предыдущей версией
- Изменение формата данных (старые файлы не работают)

**Примеры:**
- Изменение формата `.playback` файлов
- Полная замена Dialog на другой UI
- Несовместимые изменения API скриптов

**Переход 0.x → 1.0.0:**
- Весь проект стабилен и готов к продакшену
- Все критичные функции работают
- API зафиксирован

---

#### MINOR (второе число)
**Когда увеличивать:**
- Новая функциональность
- Обратная совместимость сохраняется
- Добавление опций (старое продолжает работать)

**Примеры:**
- ✅ Добавление автосохранения
- ✅ Настройки сериалов UI
- ✅ Новые CEC кнопки
- ✅ Skip intro/outro
- ✅ Интеграция SQLite

**Формула:** `0.4.0` → `0.5.0`

---

#### PATCH (третье число)
**Когда увеличивать:**
- Исправления багов
- Оптимизация без новой функциональности
- Мелкие улучшения

**Примеры:**
- 🐛 Исправление `extract_series_key()`
- 🐛 Исправление RED/GREEN кнопок CEC
- ⚡ Оптимизация автосохранения
- 📝 Улучшение отображения в меню

**Формула:** `0.5.0` → `0.5.1`

---

## 🏷️ GitHub тегирование

### Правила создания тегов

**Создаём тег при каждом MINOR релизе:**
```bash
v0.5.0 - Автосохранение прогресса
v0.6.0 - Интеграция SQLite
v0.7.0 - Skip intro/outro для сериалов
```

**PATCH релизы:**
- Теги опциональны
- Можно тегировать важные багфиксы

---

### Команды Git

```bash
# 1. Финализировать изменения
git add .
git commit -m "Release v0.5.0: Автосохранение прогресса"

# 2. Создать аннотированный тег
git tag -a v0.5.0 -m "Версия 0.5.0 - добавлено автосохранение каждые 60 секунд"

# 3. Push на GitHub
git push origin main
git push origin v0.5.0

# Или push всех тегов сразу:
# git push --tags
```

---

## 📚 Версии библиотек

> [!NOTE]
> Разные файлы могут иметь разные версии

**Библиотеки могут отставать:**
- Стабильные библиотеки остаются на текущей версии
- Обновляются только при изменениях

**Пример:**
```bash
vlc-cec.sh:         v0.7.0  # Активное развитие
db-manager.sh:      v0.1.2  # Стабильна, редко меняется
playback-tracker.sh: v0.5.0  # Периодические обновления
video-menu.sh:      v0.8.0  # Активное развитие
serials.sh:         v0.3.0  # Адаптация под SQLite
```

---

## 🎯 Критерии готовности к релизу

Релиз выпускается **по команде пользователя**, когда:

- ✅ Завершён и протестирован блок задач
- ✅ Все изменения задокументированы в CHANGELOG
- ✅ Версия в файлах обновлена
- ✅ Критичные файлы протестированы
- ✅ Нет известных критичных багов

---

## 📝 Процесс релиза

### Шаг 1: Подготовка

```bash
# 1. Проверить текущие версии
grep "# Version:" vlc-cec.sh
grep "# Version:" video-menu.sh
grep "# Version:" playback-tracker.sh

# 2. Определить новый номер версии
# MINOR: новая функция (0.5.0 → 0.6.0)
# PATCH: багфикс (0.5.0 → 0.5.1)
```

### Шаг 2: Обновление версий

```bash
# Обновить версию в изменённых файлах:
# vlc-cec.sh
# Version: 0.6.0  ← изменить

# Обновить CHANGELOG.md
# Переименовать [Unreleased] → [0.6.0] - 2025-12-01
```

### Шаг 3: Создание RELIS версий

```bash
TODAY=$(date +%y%m%d)
TIMESTAMP=$(date +%y%m%d_%H%M)

# Создать папку для релиз-бэкапов
mkdir -p BAK/RELIS/$TODAY

# Для каждого изменённого файла:

# 1. ОБЯЗАТЕЛЬНО: Бэкап текущего RELIS
cp RELIS/vlc-cec_RELIS.sh BAK/RELIS/$TODAY/vlc-cec_RELIS_${TIMESTAMP}.bak

# 2. Обновление RELIS
cp vlc-cec.sh RELIS/vlc-cec_RELIS.sh

echo "✓ RELIS обновлён: vlc-cec_RELIS.sh → v0.6.0"
```

### Шаг 4: Обновление документации

```bash
# 1. Финализировать CHANGELOG.md
vim DOCS/CHANGELOG.md

# 2. Обновить HANDOFF.md (если нужно)
# - Текущий статус проекта
# - Список завершённых задач
```

### Шаг 5: Git commit и тег

```bash
# 1. Коммит изменений
git add .
git commit -m "Release v0.6.0: Интеграция SQLite"

# 2. Создать тег
git tag -a v0.6.0 -m "Версия 0.6.0 - добавлена SQLite БД для хранения прогресса"

# 3. Push на GitHub
git push origin main
git push origin v0.6.0
```

### Шаг 6: GitHub Release (опционально)

1. Зайти на GitHub → Releases → Create new release
2. Выбрать тег: `v0.6.0`
3. Заголовок: `v0.6.0 - SQLite Integration`
4. Описание: скопировать из CHANGELOG.md
5. Publish release

---

## 📋 Checklist релиза

- [ ] Версия обновлена в изменённых файлах
- [ ] CHANGELOG.md финализирован (Unreleased → [0.x.0])
- [ ] RELIS бэкапы созданы (BAK/RELIS/YYMMDD/)
- [ ] RELIS файлы обновлены
- [ ] HANDOFF.md обновлён (статус проекта)
- [ ] Git commit создан
- [ ] Git tag создан
- [ ] Push на GitHub выполнен
- [ ] GitHub Release создан (опционально)
- [ ] Проверка после релиза

---

## 💡 Примеры сообщений

### Commit message:
```
Release v0.6.0: Интеграция SQLite

- Добавлена db-manager.sh для работы с БД
- Адаптирован playback-tracker.sh под SQLite
- Миграция данных из .playback файлов

Fixes: #12, #15
```

### Git tag annotation:
```
Версия 0.6.0 - SQLite интеграция

Основные изменения:
- SQLite БД для хранения прогресса и настроек
- Улучшена производительность
- Централизованное хранение данных

См. CHANGELOG.md для деталей
```

---

## 🔄 После релиза

1. **Создать новую секцию в CHANGELOG:**
```markdown
## [Unreleased]
### Added
### Changed
### Fixed
```

2. **Обновить HANDOFF.md:**
- Отметить релиз в истории
- Обновить статус проекта

3. **Уведомить** (если нужно):
- GitHub Discussions
- README.md (если это мажорный релиз)

---

См. также:
- [HANDOFF.md](./HANDOFF.md) - Главный файл проекта
- [BACKUP_GUIDE.md](./BACKUP_GUIDE.md) - Руководство по бэкапам
- [CHANGELOG.md](../../CHANGELOG.md) - История изменений
