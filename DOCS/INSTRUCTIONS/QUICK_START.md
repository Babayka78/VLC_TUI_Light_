# QUICK_START - Быстрый старт новой сессии

**Версия:** 1.1  
**Дата:** 04.12.2025

---

> [!NOTE]
> **📚 Навигация:** [INDEX.md](./INDEX.md) | [HANDOFF.md](./HANDOFF.md) | [SESSION-START.md](./SESSION-START.md)

---

## 📨 Формат первого сообщенияи агенту:**

```
Привет! Продолжаем работу над VLC проектом.

Прочитай:
2. DOCS/CHANGELOG.md - последние изменения

Текущая задача: [описать что нужно сделать]
```

---

## 📋 Если агент забыл контекст:

**Напомни структуру:**
```
Проект: VLC Media Player для Raspberry Pi
- Язык: Bash
- UI: Dialog (TUI)
- Управление: CEC пульт ТВ
- БД: SQLite (в процессе миграции)

Документация:
- DOCS/INSTRUCTIONS/ - рабочие инструкции (для агента)
- DOCS/reference/ - публичная документация
```

---

## ⚡ Краткий чеклист агента:

**Агент должен в начале сессии:**
1. ✅ Прочитать `DOCS/INSTRUCTIONS/HANDOFF.md` (ПОЛНОСТЬЮ)
2. ✅ Посмотреть приоритетные задачи в `QUICK_START.md`
3. ✅ Проверить токены (должно быть > 150,000)
4. ✅ НЕ читать CHANGELOG автоматически (только по запросу)

---

## 🎯 Приоритетные задачи (сейчас):

**1. ✅ ЗАВЕРШЕНО - Миграция на явное поле статуса (02.12.2025)**

**Что сделано:**
- Добавлено поле `status` в таблицу `playback` (БД SQLite)
- Статусы: `NULL` (не просмотрено), `'partial'` (1-94%), `'watched'` (95-100%), `'sleep'` (будущее)
- Кеш переключен на статусы: `PLAYBACK_STATUS_CACHE` вместо `PLAYBACK_PERCENT_CACHE`  
- Функции: `get_playback_status()`, `get_playback_batch_status()`, CLI команды
- Исправлен баг отображения статусов после v0.4.0
- Производительность: ~3 секунды для 32 файлов (BUILD_LIST: 0.3-0.4s)

**Файлы обновлены:**
- `vlc_db.py` v1.1.0 - поле status, методы, CLI
- `db-manager.sh` v0.2.1 - обёртки для статусов
- `playback-tracker.sh` v0.3.2 - кеш статусов
- `video-menu.sh` - прямой доступ к кешу

---

**2. 🟡 СРЕДНИЙ ПРИОРИТЕТ - Skip Intro/Outro + Autoplay**

**2. 🟡 Средний приоритет:**


**2.2. Autoplay:**
- Реализовать автоматический переход к следующей серии
- Интеграция с vlc-cec.sh
- Использовать настройки из БД (auto_continue)

**2. 🔵 Низкий приоритет:**
- Исправить CEC кнопки:
  - RED - переключение аудиодорожек (работает некорректно)
  - GREEN - переключение субтитров (работает некорректно)
- Запрос о продолжении просмотра (при позиции > 5%)
- Оптимизация автосохранения

---

## � Будущие возможности (Future Features)

Долгосрочные планы и концепции находятся в `/DOCS/future_features/`:
- **OSD уведомления** - визуальные уведомления в VLC
- **Ambilight** - подсветка за экраном синхронизированная с видео
- **Performance optimization** - оптимизация производительности
- **Sleep protection** - защита от засыпания
- **Development roadmap** - общая дорожная карта проекта

> [!NOTE]
> Эти задачи не являются приоритетными и рассматриваются после завершения критичных задач.

---

## 🔧 TODO: Расширенная диагностика CEC

> [!NOTE]
> Задачи для улучшения диагностики потери CEC после SSH disconnect/reconnect

### Для `Test/checkcec.sh` (на будущее):

**1. Проверка PID из переменной:**
```bash
# Если vlc-cec.sh экспортирует $CEC_PID
if [ -n "$CEC_PID" ]; then
    kill -0 $CEC_PID 2>/dev/null
fi
```

**2. Проверка доступности устройства:**
```bash
if [ -e "/dev/cec1" ]; then
    ls -la /dev/cec1
    lsof /dev/cec1  # Кто держит устройство
fi
```

**3. Проверка VLC процесса для сравнения:**
```bash
ps aux | grep '[c]vlc'
netstat -tuln | grep 4212  # RC интерфейс
```

**4. Логирование состояния pipe:**
```bash
# Проверить file descriptors
lsof -p $CEC_PID | grep pipe
```

### Примечания:
- **FIFO:** Не работает корректно → используем pipe
- **Watchdog:** Дополнительный процесс в памяти → требует обдумывания
- **Полное логирование:** Раздует код и потребует много изменений → отложено

**Текущий статус:** Создан базовый [`Test/checkcec.sh`](file://../../Test/checkcec.sh) - проверка наличия процесса

---

## 💡 Полезные команды:

```bash
# Просмотр текущего статуса
git status

# Последние изменения
git log --oneline -5

# Токены агента
# Агент сам показывает остаток
```

---

**Этого достаточно для быстрого старта!** 🚀
